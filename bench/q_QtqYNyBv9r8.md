---
# Questions, "Silent Coding Session - Rust Lang - 1" (12:07)
Answer all five. State your answer and the evidence (timestamps and/or frame filenames).
If your evidence does not determine an answer, say "insufficient evidence" rather than
guessing. A confident wrong answer scores worse than an honest gap.
---

1. A web page is open in the browser on the left side of the screen for the whole video. What is the title shown in that page's header bar, and what is the domain in the address bar?

2. Once the user is inside a cargo project directory, the shell prompt shows a git branch name in parentheses. What is the branch name, and does the same branch name also appear in the VS Code status bar?

3. The very first program of the session (hello_world/main.rs, compiled with rustc) is run from the terminal with ./main. What is the exact string it prints? Quote it character for character.

4. The user runs `cargo --version` in the terminal. What is the full version line that is printed (version number plus the parenthesised commit hash and date)?

5. While inside the target/release directory of the hello_cargo project, the user runs a command that produces a long flood of "command not found", "parse error" and "bad pattern" lines. What exact command was typed, and what does the final job-status line at the bottom of that output say (job number, PID, and exit code)?
