---
# Questions, "I Code HTML & CSS Without Saying a Word" (28:17)
Answer all five. State your answer and the evidence (timestamps and/or frame filenames).
If your evidence does not determine an answer, say "insufficient evidence" rather than
guessing. A confident wrong answer scores worse than an honest gap.
---

1. What exact URL (host, port and path) is shown in the browser's address bar for the page being built?

2. In the `index.html` file open in the editor (also visible in the DevTools Elements panel), what is the value of the `lang` attribute on the `<html>` element?

3. The stylesheet contains a media query that hides `.header__nav`. What `max-width` value (in px) does that `@media` rule use?

4. At one point the author edits the `.header__nav-btn span` rule directly inside Chrome DevTools (Styles panel), not in the editor. In that DevTools-edited rule, what `width` value and what `background-color` value are shown?

5. Near the end of the video a second `@media` block is added below the first one. What `max-width` value does it use, and which selector is the first rule written inside it?
