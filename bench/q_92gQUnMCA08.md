---
# Questions, "10 Hour Coding Time Lapse (C programming)" (6:10)
Answer all five. State your answer and the evidence (timestamps and/or frame filenames).
If your evidence does not determine an answer, say "insufficient evidence" rather than
guessing. A confident wrong answer scores worse than an honest gap.
---

1. Which code editor is being used throughout the video, what workspace name appears in the window title bar, and what is the exact name of the root folder shown at the top of the Explorer sidebar?

2. The root folder in the Explorer sidebar contains a set of numbered subfolders. How many numbered subfolders are there, and what are the exact labels of the first and the last one?

3. At one point the programmer types a nonsense command into the integrated terminal and the shell reports an error. What exactly was typed, and what is the first line of the error message the shell prints?

4. In one of the later files, a program that reads integers from a text file floods the terminal with the same number repeated over and over. What number is repeated, what is the exact loop condition in the source code that is visible at that moment, and what filename is passed to fopen?

5. In the folder "5. Almost intermediate", the programmer writes a frustrated all-caps comment inside Question-2.c after a printf that uses a math library function. Quote the exact text of that all-caps comment line (the first one), and name the library function called in the printf directly above it.
